export interface License {
  licenseId: string; // generate by uuid.
  licenseName: string;
  productId: string;
  licenseDetails: string;
  creatorId: string;
  ownerId: string;
  assetType: string;
}
